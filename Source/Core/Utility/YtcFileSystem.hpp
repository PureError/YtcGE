#ifndef YTC_FILE_SYSTEM_HPP
#define YTC_FILE_SYSTEM_HPP

#include "../Utility/Localization.hpp"
#include "../Fundation.hpp"
namespace YtcGE
{
    namespace filesystem
    {
        class Path
        {
        //public:
        //    static String Combine(const String& lhs, const String& rhs);
        //    static String Combine(const std::vector<String>& paths);
        //    static String GetFileName(const String & path);
        //    static String GetExtension(const String & path);
        //    static String GetFileNameWithoutExtension(const String& path);
        //    static String GetDirectoryName(const String& path);
        //    static String FullPath(const String& path);
        //    static bool EndsInDirectorySeparator(const String& path);
        //    static String TrimEndingDirectorySeparator(const String& path);
        };
    }
}


#endif